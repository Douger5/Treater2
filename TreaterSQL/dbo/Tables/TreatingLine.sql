CREATE TABLE [dbo].[TreatingLine] (
    [TreatingLineID] INT            NOT NULL,
    [TreaterName]    NVARCHAR (MAX) NULL,
    CONSTRAINT [PK_dbo.TreatingLine] PRIMARY KEY CLUSTERED ([TreatingLineID] ASC)
);

